# Executáveis gerados por `scripts/build.sh` — dezenas de MB, reproduzíveis
# a qualquer momento, não pertencem ao git.
GesConApp
GesConApp-*

# Worktrees do superpowers
.worktrees/

# IDE
.vscode/
.idea/
